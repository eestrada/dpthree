language: none

env:
    - PYTHON_VERSION=2.7
    - PYTHON_VERSION=3.4

build:
#  pre_ci:
#    - docker pull "python:${PYTHON_VERSION}"

  pre_ci_boot:
    image_name: python
    image_tag: "${PYTHON_VERSION}"
    pull: true

  ci:
    - make test
