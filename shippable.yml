language: none

env:
    - PYTHON_VERSION=2.6
    - PYTHON_VERSION=2.7
    - PYTHON_VERSION=3.3
    - PYTHON_VERSION=3.4
    - PYTHON_VERSION=3.5
    - PYTHON_VERSION=3.6

build:
  pre_ci:
    - docker pull "python:${PYTHON_VERSION}"

  ci:
    - make test
